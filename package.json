{
  "name": "elementary-electron-boilerplate",
  "version": "0.1.0",
  "description": "Boilerplate(s) to run the elementary audio engine within an electron app.",
  "main": "./electron/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nick-thompson/elementary": "^0.9.5"
  },
  "devDependencies": {
    "electron": "^12.0.5",
    "nodemon": "^2.0.7"
  },
  "nodemonConfig": {
    "watch": [
      "**/*.js",
      "./public/*.*"
    ],
    "exec": "npm run start"
  }
}
